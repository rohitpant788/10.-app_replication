# Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit the actual .env file to Git!

#===========================================
# Database Configuration (Neon PostgreSQL)
#===========================================
DB_URL=jdbc:postgresql://your-neon-host.aws.neon.tech/neondb?sslmode=require
DB_USERNAME=neondb_owner
DB_PASSWORD=your-secure-password

#===========================================
# GitLab CI/CD Variables
# Set these in GitLab: Settings → CI/CD → Variables
#===========================================
# DB_URL (same as above)
# DB_USERNAME (same as above)
# DB_PASSWORD (same as above)
# KUBECONFIG_CONTENT (base64 encoded kubeconfig file)

#===========================================
# Docker Registry
#===========================================
# For local development
REGISTRY_URL=localhost:30500

# For GitLab (if using GitLab container registry)
# REGISTRY_URL=gitlab.local:5050

#===========================================
# Service Ports (for local development)
#===========================================
DATA_SERVICE_PORT=9090
FILE_SERVICE_PORT=9091
REFDATA_SERVICE_PORT=9092
SEARCH_SERVICE_PORT=9093
UI_PORT=3000

#===========================================
# Kubernetes Configuration
#===========================================
KUBE_NAMESPACE=default
HELM_RELEASE_NAME=app

#===========================================
# SMTP Configuration (for local testing)
#===========================================
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_WEB_PORT=1080

#===========================================
# Application Configuration
#===========================================
SPRING_PROFILES_ACTIVE=local
LOGGING_LEVEL=INFO

#===========================================
# URLs (for local development)
#===========================================
GITLAB_URL=http://gitlab.local:8080
APP_URL=http://app.local
API_BASE_URL=http://app.local

#===========================================
# Security (for production - use strong values!)
#===========================================
# JWT_SECRET=change-this-to-a-strong-secret
# ENCRYPTION_KEY=change-this-to-a-strong-key
