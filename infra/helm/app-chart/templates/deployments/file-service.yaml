{{- if .Values.fileService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.fileService.name }}
  labels:
    {{- include "app.serviceLabels" .Values.fileService.name | nindent 4 }}
spec:
  replicas: {{ .Values.fileService.replicas }}
  {{- with .Values.fileService.strategy }}
  strategy:
    type: {{ .type }}
    rollingUpdate:
      maxSurge: {{ .maxSurge }}
      maxUnavailable: {{ .maxUnavailable }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "app.serviceSelectorLabels" .Values.fileService.name | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app.serviceSelectorLabels" .Values.fileService.name | nindent 8 }}
    spec:
      containers:
      - name: {{ .Values.fileService.name }}
        image: "{{ .Values.fileService.image.repository }}:{{ .Values.fileService.image.tag }}"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.fileService.port }}
          name: http
        env:
        # Non-sensitive config from ConfigMap
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: {{ .Values.fileService.name }}-config
              key: SPRING_PROFILES_ACTIVE
        - name: LOGGING_LEVEL_ROOT
          valueFrom:
            configMapKeyRef:
              name: {{ .Values.fileService.name }}-config
              key: LOGGING_LEVEL_ROOT
        - name: DATA_SERVICE_BASE-URL
          valueFrom:
            configMapKeyRef:
              name: {{ .Values.fileService.name }}-config
              key: DATA_SERVICE_BASE-URL
        # Database credentials from Secret
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DATABASE_URL
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DATABASE_USERNAME
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DATABASE_PASSWORD
        # Health probes disabled - actuator endpoints not working
        # Resource limits
        resources:
          {{- toYaml .Values.fileService.resources | nindent 10 }}
{{- end }}
